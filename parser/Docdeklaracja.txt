The Language deklaracja
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of deklaracja==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
Integer literals //Integer// are nonempty sequences of digits.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in deklaracja are the following:
  | ``auto`` | ``bool`` | ``break`` | ``case``
  | ``continue`` | ``default`` | ``else`` | ``false``
  | ``for`` | ``if`` | ``int`` | ``print``
  | ``return`` | ``string`` | ``switch`` | ``true``
  | ``while`` |  |  | 

The symbols used in deklaracja are the following:
  | ; | { | } | [
  | ] | = | , | (
  | ) | : | || | &&
  | == | != | < | >
  | <= | >= | + | -
  | * | / | ++ | --
  | ! | *= | /= | +=
  | -= |  |  | 

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of deklaracja==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //Program// | -> | //[Compund_content]// 
  | //Compund_content// | -> | //Stm// 
  |  |  **|**  | //Dec// 
  |  |  **|**  | //Exp2// ``;`` 
  |  |  **|**  | //Namespace// 
  | //Namespace// | -> | ``{`` //[Compund_content]// ``}`` 
  |  |  **|**  | ``{`` ``}`` 
  | //[Compund_content]// | -> | //Compund_content// 
  |  |  **|**  | //Compund_content// //[Compund_content]// 
  | //Dec// | -> | //Variable// ``;`` 
  |  |  **|**  | //Function// 
  | //Type_specifier// | -> | ``bool`` 
  |  |  **|**  | ``int`` 
  |  |  **|**  | ``string`` 
  |  |  **|**  | ``auto`` 
  | //Dec_base// | -> | //Type_specifier// //Ident// 
  | //Variable// | -> | //Initialized_variable// 
  |  |  **|**  | //Uninitialized_variable// 
  | //Uninitialized_variable// | -> | //Dec_base// 
  |  |  **|**  | //Dec_base// ``[`` //Exp2// ``]`` 
  | //Initialized_variable// | -> | //Dec_base// ``=`` //Initializer// 
  |  |  **|**  | //Dec_base// ``[`` ``]`` ``=`` ``{`` //[Initializer]// ``}`` 
  |  |  **|**  | //Dec_base// ``=`` ``{`` //[Initializer]// ``}`` 
  | //Initializer// | -> | //Exp2// 
  | //[Initializer]// | -> | //Initializer// 
  |  |  **|**  | //Initializer// ``,`` //[Initializer]// 
  | //Function// | -> | //Dec_base// ``(`` ``)`` //Namespace// 
  |  |  **|**  | //Dec_base// ``(`` //[Param]// ``)`` //Namespace// 
  | //[Param]// | -> | //Param// 
  |  |  **|**  | //Param// ``,`` //[Param]// 
  | //Param// | -> | //Uninitialized_variable// 
  | //Stm// | -> | //Selection_stm// 
  |  |  **|**  | //Iter_stm// 
  |  |  **|**  | //Jump_stm// 
  |  |  **|**  | //Print_stm// 
  | //Selection_stm// | -> | ``if`` ``(`` //Exp// ``)`` //Compund_content// 
  |  |  **|**  | ``if`` ``(`` //Exp// ``)`` //Compund_content// ``else`` //Compund_content// 
  |  |  **|**  | ``switch`` ``(`` //Exp// ``)`` //Switch_content// 
  |  |  **|**  | ``switch`` ``(`` //Exp// ``)`` ``{`` //[Switch_content]// ``}`` 
  | //Switch_content// | -> | ``case`` //Exp// ``:`` //[Compund_content]// 
  |  |  **|**  | ``default`` ``:`` //[Compund_content]// 
  | //[Switch_content]// | -> | //Switch_content// 
  |  |  **|**  | //Switch_content// //[Switch_content]// 
  | //Iter_stm// | -> | ``while`` ``(`` //Exp// ``)`` //Compund_content// 
  |  |  **|**  | ``for`` ``(`` //Exp_or_empty// ``;`` //Exp_or_empty// ``;`` //Exp_or_empty// ``)`` //Compund_content// 
  | //Exp_or_empty// | -> | **eps**
  |  |  **|**  | //Exp// 
  | //Jump_stm// | -> | ``continue`` ``;`` 
  |  |  **|**  | ``break`` ``;`` 
  |  |  **|**  | ``return`` //Exp// ``;`` 
  | //Print_stm// | -> | ``print`` ``(`` //Exp2// ``)`` ``;`` 
  | //Exp// | -> | //Exp// ``,`` //Exp2// 
  |  |  **|**  | //Exp1// 
  | //Exp2// | -> | //LValue// //Assignment_op// //Exp2// 
  |  |  **|**  | //Exp3// 
  | //Exp4// | -> | //Exp4// ``||`` //Exp5// 
  |  |  **|**  | //Exp5// 
  | //Exp5// | -> | //Exp5// ``&&`` //Exp6// 
  |  |  **|**  | //Exp6// 
  | //Exp9// | -> | //Exp9// ``==`` //Exp10// 
  |  |  **|**  | //Exp9// ``!=`` //Exp10// 
  |  |  **|**  | //Exp10// 
  | //Exp10// | -> | //Exp10// ``<`` //Exp11// 
  |  |  **|**  | //Exp10// ``>`` //Exp11// 
  |  |  **|**  | //Exp10// ``<=`` //Exp11// 
  |  |  **|**  | //Exp10// ``>=`` //Exp11// 
  |  |  **|**  | //Exp11// 
  | //Exp12// | -> | //Exp12// ``+`` //Exp13// 
  |  |  **|**  | //Exp12// ``-`` //Exp13// 
  |  |  **|**  | //Exp13// 
  | //Exp13// | -> | //Exp13// ``*`` //Exp14// 
  |  |  **|**  | //Exp13// ``/`` //Exp14// 
  |  |  **|**  | //Exp14// 
  | //Exp15// | -> | ``++`` //LValue// 
  |  |  **|**  | ``--`` //LValue// 
  |  |  **|**  | //Unary_operator// //Exp14// 
  |  |  **|**  | //Exp16// 
  | //Exp16// | -> | //LValue// ``++`` 
  |  |  **|**  | //LValue// ``--`` 
  |  |  **|**  | //Exp17// 
  | //Exp17// | -> | //Ident// ``(`` ``)`` 
  |  |  **|**  | //Ident// ``(`` //[Exp2]// ``)`` 
  |  |  **|**  | //LValue// 
  |  |  **|**  | //Constant// 
  |  |  **|**  | ``(`` //Exp// ``)`` 
  | //[Exp2]// | -> | //Exp2// 
  |  |  **|**  | //Exp2// ``,`` //[Exp2]// 
  | //Constant// | -> | //CBool// 
  |  |  **|**  | //Integer// 
  |  |  **|**  | //String// 
  | //CBool// | -> | ``true`` 
  |  |  **|**  | ``false`` 
  | //LValue// | -> | //Ident// 
  |  |  **|**  | //Ident// ``[`` //Exp// ``]`` 
  | //Constant_expression// | -> | //Exp3// 
  | //Exp1// | -> | //Exp2// 
  | //Exp3// | -> | //Exp4// 
  | //Exp6// | -> | //Exp7// 
  | //Exp7// | -> | //Exp8// 
  | //Exp8// | -> | //Exp9// 
  | //Exp11// | -> | //Exp12// 
  | //Exp14// | -> | //Exp15// 
  | //Unary_operator// | -> | ``+`` 
  |  |  **|**  | ``-`` 
  |  |  **|**  | ``!`` 
  | //Assignment_op// | -> | ``=`` 
  |  |  **|**  | ``*=`` 
  |  |  **|**  | ``/=`` 
  |  |  **|**  | ``+=`` 
  |  |  **|**  | ``-=`` 


